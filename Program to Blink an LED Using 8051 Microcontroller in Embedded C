//Program to Blink an LED Using 8051 Microcontroller in Embedded C


#include <reg51.h>

sbit led = P2^1;

void delay()
{
    int i,j;
    for(i=0;i<1000;i++)
        {for(j=0;j<1000;j++);
					{
					}
				}
}

void main()
{
    while(1)
    {
       led = 1;   // ON
        delay();

        led = 0;   // OFF
        delay();
    }
}
